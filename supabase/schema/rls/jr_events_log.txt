MIGAWKA STANU DOSTĘPU — public.jr_events_log
⚠️ Zapis stanu produkcji, NIE migracja i NIE skrypt do wykonania.
   Patrz README.md w tym katalogu.

RLS            : WŁĄCZONE
FORCE RLS      : nie

UPRAWNIENIA (tabelowe)
  anon           : — brak
  authenticated  : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE
  postgres       : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE
  service_role   : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE

UPRAWNIENIA (kolumnowe, ponad tabelowymi)
  — brak

POLITYKI (1)

  [1] jr_events_own
      polecenie : ALL   rodzaj: PERMISSIVE
      role      : public
      warunek   : (athlete_id IN ( SELECT a.id FROM (jr_athletes a JOIN jr_players p ON ((p.id = a.player_id))) WHERE (p.user_id = auth.uid())))
      przy zapisie: (athlete_id IN ( SELECT a.id FROM (jr_athletes a JOIN jr_players p ON ((p.id = a.player_id))) WHERE (p.user_id = auth.uid())))
