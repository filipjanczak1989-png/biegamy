MIGAWKA STANU DOSTĘPU — public.game_events
⚠️ Zapis stanu produkcji, NIE migracja i NIE skrypt do wykonania.
   Patrz README.md w tym katalogu.

RLS            : WŁĄCZONE
FORCE RLS      : nie

UPRAWNIENIA (tabelowe)
  anon           : — brak
  authenticated  : — brak
  postgres       : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE
  service_role   : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE

UPRAWNIENIA (kolumnowe, ponad tabelowymi)
  anon / INSERT na kolumnach:
    anon_id, dane, typ, wersja
  authenticated / INSERT na kolumnach:
    anon_id, athlete_id, dane, typ, wersja

POLITYKI (1)

  [1] ge_insert
      polecenie : INSERT   rodzaj: PERMISSIVE
      role      : anon, authenticated
      przy zapisie: true
