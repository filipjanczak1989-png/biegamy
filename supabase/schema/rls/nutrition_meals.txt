MIGAWKA STANU DOSTĘPU — public.nutrition_meals
⚠️ Zapis stanu produkcji, NIE migracja i NIE skrypt do wykonania.
   Patrz README.md w tym katalogu.

RLS            : WŁĄCZONE
FORCE RLS      : nie

UPRAWNIENIA (tabelowe)
  anon           : — brak
  authenticated  : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE
  postgres       : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE
  service_role   : DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE

UPRAWNIENIA (kolumnowe, ponad tabelowymi)
  — brak

POLITYKI (8)

  [1] coach_deletes_athletes_meals
      polecenie : DELETE   rodzaj: PERMISSIVE
      role      : public
      warunek   : (EXISTS ( SELECT 1 FROM athletes a WHERE ((a.id = nutrition_meals.athlete_id) AND (a.coach_id = auth.uid()))))

  [2] coach_inserts_athletes_meals
      polecenie : INSERT   rodzaj: PERMISSIVE
      role      : public
      przy zapisie: (EXISTS ( SELECT 1 FROM athletes a WHERE ((a.id = nutrition_meals.athlete_id) AND (a.coach_id = auth.uid()))))

  [3] coach_reads_athletes_meals
      polecenie : SELECT   rodzaj: PERMISSIVE
      role      : public
      warunek   : (EXISTS ( SELECT 1 FROM athletes a WHERE ((a.user_id = nutrition_meals.athlete_id) AND (a.coach_id = auth.uid()))))

  [4] coach_updates_athletes_meals
      polecenie : UPDATE   rodzaj: PERMISSIVE
      role      : public
      warunek   : (EXISTS ( SELECT 1 FROM athletes a WHERE ((a.id = nutrition_meals.athlete_id) AND (a.coach_id = auth.uid()))))

  [5] users_delete_own_meals
      polecenie : DELETE   rodzaj: PERMISSIVE
      role      : public
      warunek   : (auth.uid() = athlete_id)

  [6] users_insert_own_meals
      polecenie : INSERT   rodzaj: PERMISSIVE
      role      : public
      przy zapisie: (auth.uid() = athlete_id)

  [7] users_read_own_meals
      polecenie : SELECT   rodzaj: PERMISSIVE
      role      : public
      warunek   : (auth.uid() = athlete_id)

  [8] users_update_own_meals
      polecenie : UPDATE   rodzaj: PERMISSIVE
      role      : public
      warunek   : (auth.uid() = athlete_id)
